#summary Creating videos with Gource.

== Recording Videos ==

You can create a video of Gource using the --output-ppm-stream option. This creates an uncompressed sequence of
screenshots in PPM format which can then be processed by another program (such as `ffmpeg`) to produce a video. The below command line will create a video at 60fps in h264 format (assumes you have ffmpeg with h264 support):

{{{
gource --output-ppm-stream - | ffmpeg -y -b 3000K -r 60 -f image2pipe -vcodec ppm -i - gource.h264
}}}

You can also adjust the output frame rate with `--output-framerate`. Note you will need to adjust the frame rate used by the encoder (eg `ffmpeg -r`) as well.

== Videos ==

A collage of different open source software projects displayed using Gource:

<wiki:video url="http://www.youtube.com/watch?v=E5xPMW5fg48">

== Miscellaneous ==

Various videos of projects created with Gource.

=== Fotowall ===

History of Fotowall development up to fall 2009:

<wiki:video url="http://www.youtube.com/watch?v=LzmvHaCNXjg"/>

=== Git ===

Gource running on the Git project itself (courtesy of Sam Vilain):

<wiki:video url="http://www.youtube.com/watch?v=GTMC3g2Xy8c"/>

=== Fiji === 

Development of Fiji (courtesy of Johannes Schindelin):

<wiki:video url="http://www.youtube.com/watch?v=ZDZvoJX3PDM"/>

=== MPlayer CE === 

MPlayer CE - an MPlayer fork:

<wiki:video url="http://www.youtube.com/watch?v=4mL1WDfbu84"/>